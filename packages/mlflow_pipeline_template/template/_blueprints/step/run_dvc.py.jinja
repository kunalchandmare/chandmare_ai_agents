#!/usr/bin/env python
"""
Pipeline step: {{ step_name }}

Artifact utilities: components/shared/artifact_utils/ (DVC backend)
"""
import argparse
import logging
import os
import sys

import mlflow

PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", ".."))
if PROJECT_ROOT not in sys.path:
    sys.path.insert(0, PROJECT_ROOT)

from components.shared.artifact_utils.dvc_artifacts import (
    track_artifact,
    pull_artifact,
    push_artifact,
)
from components.shared.artifact_utils.file_utils import read_one_csv_to_df, empty_dir

logging.basicConfig(level=logging.INFO, format="%(asctime)-15s %(message)s")
logger = logging.getLogger()


def go(args):
    with mlflow.start_run():
        # pull_artifact(args.input_artifact)
        # TODO: implement step logic
        pass
        # track_artifact("<output_path>")
        # push_artifact("<output_path>")


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="Step: {{ step_name }}")
    parser.add_argument("--input_artifact", type=str, required=True, help="Local path (DVC-tracked)")
    parser.add_argument("--output_artifact", type=str, required=True, help="Output path")
    parser.add_argument("--output_type", type=str, required=True, help="Artifact type tag")
    parser.add_argument("--output_description", type=str, required=True, help="Description")
    args = parser.parse_args()
    go(args)

